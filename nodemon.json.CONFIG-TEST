{
  "watch": ["src"],
  "ext": "ts,js,json",
  "exec": "concurrently \"npx tsc --watch\" \"ts-node src/index.ts\"",
  "exclude": ["node_modules"]
}
//  "concurrently", which facilitates the execution of multiple commands, such as nodemon, npx, tsc, etc., allowing you to combine different functionalities.
